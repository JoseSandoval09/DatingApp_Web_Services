meta {
  name: Login - Unathorized Bad Password
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/account/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "test@test.com",
    "password": ""
  }
}

tests {
  test("should return unauthorized ", function() {
    expect(res.getStatus()).to.equal(401);
  })
  
  
}

settings {
  encodeUrl: true
  timeout: 0
}
